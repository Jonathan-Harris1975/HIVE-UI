// Compatibility shim: the API contract has a single source of truth in src/types/api.ts.
// Keep worker-side imports pointing here if needed without maintaining a second copy.
export * from '../../src/types/api'
