Delete from the repository if present:
public/_redirects

Reason:
HIVE-UI is now deployed as Cloudflare Worker + Static Assets.
The old Pages-style SPA redirect is invalid for Worker Static Assets and can cause
Cloudflare error 100324 (infinite redirect loop).

The build now also defensively removes dist/_redirects if a stale copy is ever
introduced by an old branch or merge.
