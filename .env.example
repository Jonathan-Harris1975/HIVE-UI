# Local Vite development only. These values remain on the Vite development server.
HIVE_API_BASE_URL=https://your-hive-service.koyeb.app
HIVE_ADMIN_TOKEN=

# Cloudflare Pages Functions production variables/secrets are configured in the
# Cloudflare dashboard, not in a VITE_ browser variable.
# HIVE_UI_ACCESS_KEY=
# HIVE_UI_SESSION_TTL_SECONDS=43200

VITE_APP_NAME=HIVE
