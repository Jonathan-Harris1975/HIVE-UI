# HIVE UI environment variables
# Copy to .env.local for local development

# URL of your HIVE backend (Koyeb or localhost)
# Leave empty to use Vite dev proxy (recommended for local dev)
VITE_HIVE_API_URL=

# For production (Cloudflare Pages):
# VITE_HIVE_API_URL=https://your-hive-app.koyeb.app
